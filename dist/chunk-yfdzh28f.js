import{s}from"/dist/chunk-sp0bb9w4.js";function e(){return s("div",{class:"markdown-body",dangerouslySetInnerHTML:{__html:`<h1>Zig/JavaScript 代码风格指南</h1><blockquote><p>部分内容源自 Zig 语言文档中的 <a href="https://ziglang.org/documentation/0.9.0/#Style-Guide">Style Guide</a>。</p></blockquote><p>每个编程语言都会有自己的风格。有的编程语言比较严格，比如 go。go 在上市的时候也带上 go fmt 命令，用于格式化代码。当时也有不少人反对官方管按语言风格。不过后来上市的编程语言都带上了自己的 fmt 工具，用于格式化代码。可以形成统一的风格对于新上手的朋友来说，当然比较好。只是对于老手，就少了展示技术的机会。</p><p>Zig 在语言文档也写了一段风格指南。第一眼看 Zig 其实有一种很 js 的感觉，带有很多 js 语言的味道。最明显的是 <code>const</code> 声明不可变变量，<code>var</code> 明显变量。还有不知道是敌是友的 <code>undefined</code>。导入外部代码的 <code>const xxx = @import('yyyy.zig')</code> 也是一股 js 味道。</p><p>下面是正文。</p><h2>Style Guide</h2><h3>空白</h3><ul><li>使用 4 个空格的缩进。</li><li><code>{</code> 在代码的同一行，除非要执行排版。</li><li>小于两个元素的列表/数组，放置到同一行，并且带上一个拖尾 <code>,</code>。</li><li>每行宽度的目标是 100 列，但是还得看场景。</li></ul><h3>命名</h3><p>大致三种：<code>camelCaseFunctionName</code>，<code>TitleCaseTypeName</code>，<code>snake_case_variable_name</code>。详情规则如下：</p><ul><li>如果 <code>x</code> 是一个 <code>type</code>（类型）应该写作 <code>TitleCase</code>。除非是一个零字段的 <code>struct</code> 而且不会被实例化，这种情况下这个 <code>struct</code> 应该是一个 <code>namespace</code>，应该写作 <code>snake_case</code>。</li><li>如果 <code>x</code> 可以调用，但是 <code>x</code> 返回的类型是 <code>type</code>，那么 <code>x</code> 应该是 <code>TitleCase</code>。</li><li>如果 <code>x</code> 可以调用，并且不是上面的情况，则写作 <code>camelCase</code>。</li><li>其他场景 <code>x</code> 应该写作 <code>snake_case</code>。</li></ul><p>名词的缩写也应该按上面的规则，即使只有两个字母。</p><p>文件名则分为两类：type、namespace。如果一个源码文件的顶层对象是一个 <code>struct</code>，并且是一个 type，用 <code>TitleCase</code> 风格，否则用 <code>snake_case</code> 风格。目录统一按 <code>snake_case</code>。</p><p>上面的就是推荐的规则。具体还得看场景。</p><h3>文件编码</h3><ul><li>使用 utf-8</li><li>换行用 <code>LF</code></li><li>代码的不使用 Ascii control characters，除非是 LF、CR、HT、U+0009 (HT): U+0000 - U+0008, U+000b - U+000c, U+000e - U+0001f, U+007f.</li><li>不能用非 ASCII Unicode 的字符结尾。</li></ul><h2>JavaScript 的风格又是如何？</h2><p>最近接手了一个 20K 行级别的应用项目，代码都是「随意流」——大小写混合、camelCase/snake_case 混搭。总结就没有没有任何的风格。</p><p>Rust 也是混合使用，但社区已经有比较完善的习惯用法，除 <code>struct</code> 名使用 <code>TitleCase</code>，其他基本上是 <code>snake_case</code>。Zig 也是三者混用。在用 Zig 写了几行代码，就可以理解为什么有这种约定——主要就是一眼可以看出哪个是方法，可以调用，哪个是变量，哪个是类型。</p><blockquote><p>Zig 的创始人也有很多 JavaScript 使用的经验，第一眼看 Zig 也是一股 JavaScript 味道。比如 <code>const std = @import('std');</code></p></blockquote><p>参考 Zig 的风格指南。我也总结了几条 js 的规则：</p><ul><li>文件名，顶层导出 default 的，使用 <code>TitleCase</code>；顶层导出多个对象的使用 <code>snake_case</code><ul><li><code>export default</code>的应该只有 class、vue 组件</li><li>有 <code>export default</code>就不应该再导出其他对象</li><li>尽量不使用 default 导出</li></ul></li><li>目录使用 <code>snake_case</code><ul><li>如何需要，一个目录的入口文件应该是 <code>index.js</code></li><li>不打算被外部使用的代码模块、文件，应该写作 <code>_snake_case</code> 或 <code>_TitleCase</code></li></ul></li><li>类 class 使用 <code>TitleCase</code></li><li>方法使用 <code>camelCase</code></li><li>变量使用 <code>camelCase</code></li><li>变量或全局变量使用 <code>SNAKE_CASE</code></li><li>代码文件统一使用 utf-8</li><li>换行用系统默认（windows 是 crlf，linux/osx 是 lf）<ul><li>git 应该设置换行为 <code>auto</code></li></ul></li><li>vue sfc 组件，使用 <code>TitleCase</code><ul><li>因为 vue sfc 组件都是 <code>export default</code>，相当于一种类型</li></ul></li></ul><p><strong>风格指南没有绝对正确的，关键是使用一套风格并且一直用</strong>。</p><hr /><ul><li>2022 年 09 月 16 日 初稿</li><li>2022 年 10 月 05 日 修改部分措辞</li></ul>`}},void 0,!1,void 0,this)}export{e as default};
